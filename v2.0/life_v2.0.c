#include <stdio.h>

int main() {
    /* set the field size */
    /* fill the field with dots/spaces */
    /* set the seed */
    /* print the field with the seed */
    /* count neighbors of every cell and store changes in the future field */
    /* apply changes to the current field and print the field */
    /* repeat steps 8 and 9 */

    return 0;
}
